import React from 'react'
import ReactDOM from 'react-dom'
import { Home } from '@shqld.dev/home'

ReactDOM.render(
    <React.StrictMode>
        <Home />
    </React.StrictMode>,
    document.getElementById('root')
)
